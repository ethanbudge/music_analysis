# requirements.txt — Lyric-Music Congruence pipeline (Python side)
#
#   pip install -r requirements.txt
#
# System dependency: ffmpeg (yt-dlp audio extraction)
#   macOS:  brew install ffmpeg
#   Linux:  sudo apt install ffmpeg
#
# MuQ-MuLan is installed separately (not on PyPI as a stable wheel):
#   pip install git+https://github.com/OpenMuQ/MuQ.git
#
# Apple Silicon: torch >= 2.1 for MPS; the pipeline forces float32 on MPS and
# sets PYTORCH_ENABLE_MPS_FALLBACK=1 automatically.

# ── Core scientific stack ──────────────────────────────────────────────
# Pin NumPy < 2: numba/torchlibrosa (laion_clap) need 1.x, and letting pip
# install 2.x then downgrade leaves a corrupt numpy/_core tree → the
# "empty_like already has a different docstring" crash when unpickling checkpoints.
numpy>=1.24,<2
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# ── Audio ──────────────────────────────────────────────────────────────
librosa>=0.10.1
soundfile>=0.12

# ── Deep learning / embeddings ─────────────────────────────────────────
torch>=2.1.0
torchaudio>=2.1.0
torchvision>=0.16.0       # imported by laion_clap
transformers>=4.37.0      # MERT (m-a-p/MERT-v1-330M, trust_remote_code)
accelerate>=0.26
laion-clap>=1.1.6         # LAION-CLAP via the official package (NOT transformers ClapModel)
nnAudio>=0.3.2            # MERT feature extractor dependency
# MuQ-MuLan: pip install git+https://github.com/OpenMuQ/MuQ.git
#
# CLAP music checkpoint (download once, then set LMC_CLAP_CKPT to its path):
#   https://huggingface.co/lukewys/laion_clap/blob/main/music_audioset_epoch_15_esc_90.14.pt

# ── Audio download ─────────────────────────────────────────────────────
yt-dlp>=2024.1.0

# ── Popularity / genre APIs ────────────────────────────────────────────
spotipy>=2.23.0
requests>=2.31
musicbrainzngs>=0.7.1     # MusicBrainz genre/tag lookup (genre ensemble)

# ── Generative arm (lmcgen): ACE-Step client + lyric-intelligibility screening ──
# ACE-Step 1.5 itself is NOT installed here — it runs as a separate uv-managed
# server we reach over HTTP (see src/lmcgen/acestep.py). faster-whisper (CTranslate2,
# CPU/int8, no torch) transcribes generated clips to score lyric word error rate.
faster-whisper>=1.0
# Optional generation backends (only if you use them; see src/lmcgen/{lyria,suno}.py):
# google-genai>=1.0    # Lyria 3 (Google) — needs GEMINI_API_KEY
# (Suno uses a third-party REST provider via `requests`, already installed.)

# ── SVS arm (lmcsvs): score generation for singing-voice synthesis ──────────────
# pyphen = accurate, dependency-free syllabification (a naive fallback runs without it).
# mido is optional (only if LMCSVS_MIDI=1 to also emit .mid alongside MusicXML).
# The MusicXML scores are rendered to vocals in Synthesizer V Studio 2 Pro / ACE Studio.
pyphen>=0.14
# mido>=1.3            # optional: MIDI export
# g2p_en>=2.1          # optional: ARPABET phonemes for the headless DiffSinger .ds export
#                      #   (a crude fallback runs without it; install for real renders)
# DiffSinger itself (OpenVPI) is installed separately in its OWN env with a voicebank —
# lmcsvs.diffsinger shells out to it; it is NOT a pip dependency here.

# ── Notebook / utilities ───────────────────────────────────────────────
jupyter>=1.0
tqdm>=4.66
